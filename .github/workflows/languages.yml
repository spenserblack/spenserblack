name: Languages

on:
  workflow_dispatch:

jobs:
  languages:
    name: Languages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: spenserblack/github-polyglot/action@bugfix/action-output
        id: github-polyglot
        with:
          format: svg
          token: ${{ secrets.PAT }}
      - name: Update languages.svg
        env:
          OUTPUT: ${{ steps.github-polyglot.outputs.output }}
        run: echo "$OUTPUT" > languages.svg
      - name: Commit changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add languages.svg
          git commit -m "Auto-commit: Update languages.svg"
          git push
