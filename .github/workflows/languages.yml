name: Languages

on:
  workflow_dispatch:

jobs:
  languages:
    name: Languages
    runs-on: ubuntu-latest
    steps:
      - uses: spenserblack/github-polyglot/action@main
        id: github-polyglot
        with:
          format: svg
